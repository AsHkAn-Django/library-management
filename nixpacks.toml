[phases.setup]
nixPkgs = ["python312Full", "openssl"]

[phases.install]
cmds = ["python3 -m pip install --upgrade pip", "python3 -m pip install -r requirements.txt"]

[phases.start]
cmd = "gunicorn library_management.wsgi --bind 0.0.0.0:$PORT"
